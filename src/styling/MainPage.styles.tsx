import styled from "styled-components";
import backgroundImage from '../images/portfolio-background.png';

export const MainPage = styled.div`
    text-align: center;
    background-image: url(${backgroundImage});
    font-family: Verdana;
`;