import React from 'react';
import { HeaderContainer } from './Header.styles';

const Header = () => {
  return (
    <HeaderContainer>
      {/* Header content */}
    </HeaderContainer>
  );
};

export default Header;